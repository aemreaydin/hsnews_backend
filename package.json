{
  "name": "hnews-node",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "2.5.1",
    "apollo-server": "^2.16.1",
    "bcryptjs": "^2.4.3",
    "dotenv": "^8.2.0",
    "graphql": "^15.3.0",
    "jsonwebtoken": "^8.5.1"
  },
  "devDependencies": {
    "@prisma/cli": "2.5.1"
  },
  "scripts": {
    "start": "node src/index.js",
    "studio": "npx prisma studio --experimental",
    "migrate:save": "npx prisma migrate save --experimental",
    "migrate:up": "npx prisma migrate up --experimental",
    "migrate:generate": "npx prisma generate",
    "migrate": "yarn migrate:save && yarn migrate:up && yarn migrate:generate"
  }
}
